import mariadb             # Importerer MariaDB biblioteket, så vi kan forbinde og interagere med databasen
import time                # Importerer tidsmodul til forsinkelse i loopet
import RPi.GPIO as GPIO    # Importerer GPIO-biblioteket for at styre Raspberry Pi's pins

# --- GPIO Setup ---
MOTOR_PIN = 17             # Definerer hvilken GPIO pin motoren er forbundet til
GPIO.setmode(GPIO.BCM)     # Biblioteket Rpi.GPIO kræver denne linje, da den ikke ved hvilke GPIO pins den skal bruge
GPIO.setup(MOTOR_PIN, GPIO.OUT)  # Sætter motorens pin som OUTPUT, så vi kan sende signal

# --- Database konfiguration ---
db_config = {              # Opretter en dictionary med alle forbindelsesoplysninger
    "user": "admin",       # Brugernavn til databasen
    "password": "admin",   # Adgangskode til databasen
    "host": "152.115.77.165",  # IP-adressen på database-serveren
    "port": 50110,         # Portnummer MariaDB lytter på
    "database": "motor"    # Navnet på databasen vi arbejder med
}

# Opret forbindelse til databasen og aktiver autocommit
# autocommit=True betyder at alle ændringer (INSERT, UPDATE osv.) automatisk gemmes uden at kalde conn.commit()
conn = mariadb.connect(**db_config, autocommit=True)
cursor = conn.cursor()     # Opretter en cursor som gør at man kan skrive i og sende kommandoer og læse

# Start scriptet
try:
    last_status = None     # Gemmer status, så vi undgår spam
    while True:            # Uendeligt loop
        cursor.execute("SELECT status FROM motor_kontrol WHERE id=1")  # Henter status fra række med id=1
        status = cursor.fetchone()[0]  # Får værdien ud af resultatet

        if status != last_status:     # Kun hvis værdien er ændret siden sidst
            if status == 1:
                GPIO.output(MOTOR_PIN, GPIO.HIGH)  # Tænder for motoren (sender 3.3V til pin)
                print("🔧 Motor TÆNDT (GPIO HIGH)")
            else:
                GPIO.output(MOTOR_PIN, GPIO.LOW)   # Slukker for motoren
                print("🔌 Motor SLUKKET (GPIO LOW)")
            last_status = status      # Opdaterer sidste status

        time.sleep(0.5)  # Venter 0.5 sekunder før næste tjek, så vi ikke belaster systemet

except KeyboardInterrupt:  # Hvis brugeren trykker Ctrl+C
    print("Stopper scriptet...")

finally:
    GPIO.cleanup()         # Rydder op i GPIO (vigtigt så pins ikke forbliver i en forkert tilstand)
    conn.close()           # Lukker forbindelsen til databasen korrekt
